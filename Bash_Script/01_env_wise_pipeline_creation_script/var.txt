[GLOBAL]
codebuild_img="aws/codebuild/standard:7.0"
github_repo_branch="dev"
github_repo_name="Omoron-DevOps"
github_token="123456"
github_user="Ravi"
dest_bucket="ravi-artifact-bucket"

[PIPELINE]
APP_NAME="${ENVIRONMENT1}-${REGION}-common-initial-setup"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=New_frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE="bsadnm"
SAM_INPUT_FILE="dasdas"
SAM_OUTPUT_FILE="dsdsa"
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"


[PIPELINE]
APP_NAME="${ENVIRONMENT}-generic-fe-create"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE="bsadnm"
SAM_INPUT_FILE="dasdas"
SAM_OUTPUT_FILE="dsdsa"
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"


[PIPELINE]
APP_NAME="${ENVIRONMENT}-generic-ec2-create"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE=""
SAM_INPUT_FILE=""
SAM_OUTPUT_FILE=""
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"



[PIPELINE]
APP_NAME="${ENVIRONMENT}-generic-init-create"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE=""
SAM_INPUT_FILE=""
SAM_OUTPUT_FILE=""
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"


[PIPELINE]
APP_NAME="${ENVIRONMENT}-generic-be-ec2-create"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE=""
SAM_INPUT_FILE=""
SAM_OUTPUT_FILE=""
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"


[PIPELINE]
APP_NAME="${ENVIRONMENT}-generic-be-create"
BUILDSPEC_FILE="generic-codepipeline-creation/buildspec.yml"
CODEBUILD_IMAGE="${codebuild_img}"
GITHUB_REPO_BRANCH="${github_repo_branch}"
GITHUB_REPO_NAME="${github_repo_name}"
GITHUB_TOKEN="${github_token}"
GITHUB_USER="${github_user}"
TAGS="Key=Environment,Value=${ENVIRONMENT} Key=project,Value=Healthcare-components Key=application,Value=components Key=module,Value=notifications-module"
PIPELINE_TYPE=frontend
BUCKET_NAME="damn8"
OBJECTKEY=""
PARAMETERS_FILE=""
SAM_INPUT_FILE=""
SAM_OUTPUT_FILE=""
SOURCE_BUCKET="ravi-pipeline-template"
DEST_BUCKET="${dest_bucket}"

